
#define HTTP_VERSION "1"

void func()
{
//     auto &response_body = http_response.response_body;
//     size_t size = 0;
//     size_t total = 0;
//     const char *start = response_body.c_str();
// while( total < response_body.size() && (size = send( sock，start + total,response_body.size(){
//         total += size;

// }
}

// void BuildHttpResponseHelper(int code)
// {
//     //http_request;//http_response;
//     auto &status_line = http_response.status_line;
//     status_line += HTTP_VERSION;
//     status_line += " ";
//     status_line = std::to_string(code);
//     status_line += " ";
//     status_line += Code2Desc(code);
//     switch (code)
//     {
//     case 404:
//         break;

//     default:
//         break;
//     }
// }
